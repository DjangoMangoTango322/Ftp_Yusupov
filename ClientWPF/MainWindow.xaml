<Window x:Class="ClientWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClientWPF"
        mc:Ignorable="d"
        Title="FTP Client" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Text="{Binding ServerIp}" Width="120" Margin="0,0,5,0"/>
            <TextBox Text="{Binding Port}" Width="60" Margin="0,0,5,0"/>
            <Button Content="Connect" Command="{Binding ConnectCommand}"/>
            <TextBlock Text="{Binding Status}" Margin="20,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding Files}" SelectedItem="{Binding SelectedFile}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Type" DisplayMemberBinding="{Binding IsDirectory, Converter={StaticResource BoolToTypeConverter}}"/>
                    <GridViewColumn Header="Size" DisplayMemberBinding="{Binding Size}"/>
                </GridView>
            </ListView.View>
            <ListView.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick" Command="{Binding OpenCommand}"/>
            </ListView.InputBindings>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Upload" Command="{Binding UploadCommand}" Margin="0,0,5,0"/>
            <Button Content="Download" Command="{Binding DownloadCommand}"/>
        </StackPanel>
    </Grid>
</Window>
