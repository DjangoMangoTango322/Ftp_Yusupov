<Window x:Class="ClientWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClientWPF"
        mc:Ignorable="d"
        Title="FTP Client" Height="600" Width="800" AllowDrop="True">
    <Window.Resources>
        <local:BoolToTypeConverter x:Key="BoolToTypeConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель подключения -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="IP:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding ServerIp}" Width="120" Margin="0,0,5,0"/>
            <Label Content=":" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Port}" Width="60" Margin="0,0,5,0"/>
            <Button Content="🔌 Connect" Command="{Binding ConnectCommand}" Width="80"/>
            <TextBlock Text="{Binding Status}" 
                       Margin="20,0,0,0" 
                       VerticalAlignment="Center"
                       Foreground="Blue"/>
        </StackPanel>


        <ListView Grid.Row="1" 
                  ItemsSource="{Binding Files}" 
                  SelectedItem="{Binding SelectedItem}" 
                  Name="FilesList">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="📁 Name" Width="300" DisplayMemberBinding="{Binding DisplayName}"/>
                    <GridViewColumn Header="Тип" Width="100" DisplayMemberBinding="{Binding IsDirectory, Converter={StaticResource BoolToTypeConverter}}"/>  <!-- ✅ Конвертер работает -->
                    <GridViewColumn Header="Размер" Width="100" DisplayMemberBinding="{Binding Size}"/>
                </GridView>
            </ListView.View>
            <ListView.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick" Command="{Binding OpenItemCommand}"/>  <!-- ✅ Исправлено: OpenCommand → OpenItemCommand -->
            </ListView.InputBindings>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="⬆️ Upload" 
                    Command="{Binding UploadCommand}" 
                    Margin="0,0,5,0"
                    Width="80"/>
            <Button Content="⬇️ Download" 
                    Command="{Binding DownloadCommand}"
                    Width="80"/>
            <Button Content="🔄 Refresh" 
                    Command="{Binding RefreshCommand}"
                    Width="80"/>
        </StackPanel>
    </Grid>
</Window>
